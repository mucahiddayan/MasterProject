This Program observes user behavior on a Web Page

usage

var obj = new Samaritan(); // start with default values

you can spy on a custom function by adding it as fallow

obj.spy(functionName,params/*as string*/, eventType /*like click*/, target)
